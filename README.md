# claude-code-cheatsheet

Useful tips and example commands for Claude

#### Starting Claude

```bash
# start with an initial prompt directly
claude "your prompt here"

# run a one-shot prompt and exit (non-interactive)
claude -p "your prompt"

# see all options
claude --help

# Bare bones
claude
```
